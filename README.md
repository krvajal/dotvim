# Installation:

    git clone git://github.com/krvajal/dotvim.git ~/.vim

Create symlinks:

    ln -s ~/.vim/vimrc ~/.vimrc
    ln -s ~/.vim/gvimrc ~/.gvimrc

Switch to the `~/.vim` directory, and fetch submodules:

    cd ~/.vim
    git submodule init
    git submodule update

# Plugins
* Dracula theme [repo](https://github.com/dracula/dracula-theme/)
* YouCompleteMe [repo]()

# More info:
This repo follows the steps suggested in this
[blog](http://vimcasts.org/episodes/synchronizing-plugins-with-git-submodules-and-pathogen/)



